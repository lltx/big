server:
  port: 8764

spring:
  application:
    name: big-auth-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  security:
    user:
      # 账号
      name: root
      # 密码
      password: root
      roles:
        - ROLE_USER
